# Metadata Filtered Retriever Default Configurations

general_purpose:
  name: General Purpose
  description: Standard metadata filtered retrieval
  config:
    top_k: 10
  recommended_for:
  - General use
